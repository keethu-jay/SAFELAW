import React from "react";

const Writer: React.FC = () => {
    return (
        <section className="max-w-md mx-auto mt-10 bg-white shadow rounded-lg p-8">
            <h1 className="text-3xl font-bold mb-4 text-gray-900">Writing</h1>
            <p className="text-gray-700 mb-4">
                corpus studio plug-in here, have to add some functionality in the layout for pop-up explanation boxes
            </p>
            {/* Add your login form below */}
        </section>
    );
};

export default Writer;
